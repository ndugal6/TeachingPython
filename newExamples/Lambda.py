
#Lambda are one time use functions

answer = lambda x: x*7
print(answer(5))

#Example You want a single button to have functionality that wont be used elsewhere
#Button(text="Click Me", command=lambda custom)